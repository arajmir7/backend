# File Persistence System (Python)

A command-line student record system demonstrating:

- JSON file persistence
- Automatic backups before writes
- Exception-safe file operations
- CRUD via CLI

## Run
```bash
python main.py
