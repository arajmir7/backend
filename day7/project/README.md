# Email Marketing System

A simple Python-based email marketing tool that:
- Reads users from CSV
- Generates coupon codes
- Sends emails using SMTP
- Tracks delivery metrics

## How to Run
1. Create `.env` with SMTP credentials
2. Add emails to `emails.csv`
3. Run:
   python main.py
